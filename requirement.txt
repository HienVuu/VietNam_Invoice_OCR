# Minimal, concise requirements for VietOCR project (Invoice Recognition)
# NOTE: imgaug requires numpy < 2.0 -> pin numpy accordingly.

numpy>=1.23.0,<2.0
opencv-python>=4.6
Pillow>=9.0
pyyaml

# PaddleOCR (choose CPU or GPU package per your environment)
paddlepaddle>=2.5.0            # or replace with paddlepaddle-gpu==<version> for CUDA
paddleocr>=2.6.0

# VietOCR / PyTorch (choose torch build for your CUDA)
torch>=1.12.0,<2.2
torchvision>=0.13.0,<0.15
vietocr>=0.1.0

# Augmentation / detection helpers
imgaug==0.4.0
pyclipper
lmdb
einops==0.2.0

# Text/metrics/geometry utilities
editdistance
python-Levenshtein
rapidfuzz
shapely

# Optional / integrations
requests
openai

# Dev / tooling (optional)
tqdm
matplotlib